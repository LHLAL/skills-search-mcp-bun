{
  "name": "skills-search-mcp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "test": "bun test",
    "check": "bun run scripts/check-sqlite.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ollama": "^0.5.10"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "sqlite-vec": "^0.1.0"
  }
}
